from nodoB import nodoB
from carpeta import carpeta
class arbolB :
	def __init__(self):
		self.raiz=None

	def crear(self,carpeta,nombre):
		if self.raiz==None:
			carpeta = nodoB(None,carpeta,None,None,None,None,None,None,None)
		else:
			temp = self.raiz
			ruta ="/"
			while True:
				if temp.c1.nombrer
				if temp.c2==None:
					if temp.c1
					temp.c2=carpeta
					return "carpeta creada en "+ruta
				elif temp.c3==None:
					te

			